<template>
  <description>Generate a calendar definition for the game world using setting context.</description>
  <systemPrompt>You are an expert worldbuilder and systems designer.
Respond with the requested information in a structured format.
Do not include markdown fences, explanations, or extra commentary.</systemPrompt>
  <generationPrompt>
Create a calendar definition for this setting:
<setting>
  <name>{{ setting.name | default('Unknown Setting', true) }}</name>
  <description>{{ setting.description | default('', true) }}</description>
  <theme>{{ setting.theme | default('', true) }}</theme>
  <genre>{{ setting.genre | default('', true) }}</genre>
  <tone>{{ setting.tone | default('', true) }}</tone>
  <magicLevel>{{ setting.magicLevel | default('', true) }}</magicLevel>
  <techLevel>{{ setting.techLevel | default('', true) }}</techLevel>
  <startingLocationType>{{ setting.startingLocationType | default('', true) }}</startingLocationType>
</setting>

Requirements:
1. Include a yearName for the calendar.
2. Include 12 months, each with a name, lengthDays (positive integer), and seasonName matching one of the season names.
3. Include 7 weekdays.
4. Include 4 seasons, each with a name, description (1-2 concise sentences on climate/atmosphere), startMonth, startDay, dayLengthMinutes (minutes of sunlight), and timeDescriptions (a list of entries each with timeOfDay as an integer hour in 24-hour format and a description of lighting conditions; always start with hour 0).
5. Include exactly 10 holidays, each with a name, description (1-2 sentences on what it commemorates and how observed), month, and day (integer, no suffixes).
6. If the setting can reasonably be assumed to be on Earth (modern, historical, or near-future Earth), use a standard Gregorian calendar:
   - Months exactly: January (31), February (28), March (31), April (30), May (31), June (30), July (31), August (31), September (30), October (31), November (30), December (31).
   - Weekdays exactly: Monday, Tuesday, Wednesday, Thursday, Friday, Saturday, Sunday.
   - Seasons exactly: Spring, Summer, Autumn, Winter.
7. If the setting is not reasonably Earth-based, keep names thematically appropriate to the setting.
8. Ensure all output is setting-coherent and internally consistent.
  </generationPrompt>
</template>
